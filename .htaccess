# Shibboleth (autenticação FEUP)
# O bloco só é activado quando mod_shib está presente (produção).
# Em localhost, o módulo não existe → Apache ignora o bloco.
<IfModule mod_shib>
  AuthType shibboleth
  ShibRequestSetting requireSession true
  Require shib-session
</IfModule>
